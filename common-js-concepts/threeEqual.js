// You should use === and you should avoid ==
// Triple equal (===) check data types and data 
const x = 1;
const y = true;

if(x ===  y) {
    console.log('they are both equal')
} else {
    console.log('they aren\'t both equal')
}